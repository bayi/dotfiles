
# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hyprland.org/Configuring/Configuring-Hyprland/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf


################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/
#monitor=VGA-1,1920x1080,0x0,1
monitor=DP-3,2560x1440,0x0,1
monitor=HDMI-A-2,2560x1440,2560x0,1
monitor=DP-2,2560x1440,5120x0,1
#monitor=HDMI-A-1,2560x1440,4480x0,1
#monitor=Unknown-1,disable
monitor=HDMI-A-3,disable

###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = alacritty
$fileManager = nautilus
$menu = fuzzel --launch-prefix='uwsm app -- '


#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:
#
# User env
## exec-once = systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
# Dbus
## exec-once = sleep 0.1 && dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP # Some fix idk
## exec-once = dbus-update-activation-environment --all
# Notifications
# exec-once = mako 
# Wallpapers
## exec-once = hyprpaper
# XDG Dekstop portal
## exec-once = systemctl --user start xdg-desktop-portal-hyprland
# exec-once = ~/.config/hypr/xdg.sh >> .xdg.sh.log 2>&1
# Polkit
## exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1 || /usr/libexec/polkit-gnome-authentication-agent-1
# Keyring
## exec-once = gnome-keyring-daemon --start --components=secrets

# Waybar & Tray
## exec-once = ~/.config/hypr/tray.sh 

## exec-once = systemctl --user restart oscontrol

# Clipboard
exec-once = wl-paste -t text --watch clipman store --no-persist

#exec-once = hypridle
#exec-once = $terminal

#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/

# env = XCURSOR_SIZE,32
# env = HYPRCURSOR_SIZE,32


#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general { 
    gaps_in = 4
    gaps_out = 4

    border_size = 1

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    #col.active_border = rgba(00a0f0ee) rgba(00f0a0ee) 45deg
    col.active_border = rgba(888888ee) rgba(666666ee) 45deg
    col.inactive_border = rgba(444444aa)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = true 

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle
}

group {
  col.border_active = rgba(ccccccee) rgba(aaaaaaee) 45deg
  col.border_inactive = rgba(446444aa)

  groupbar {
    font_size = 10
    text_color = rgba(ffffffff)
    col.active = rgba(0060a0ee)
    col.inactive = rgba(01010180)
  }
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 10

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 1.0

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 3
        passes = 1
        
        vibrancy = 0.1696
    }

    shadow {
      enabled = true
      range = 4
      render_power = 3
      color = rgba(1a1a1aee)
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = true

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
    smart_split = true
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
  #  new_is_master = true
  new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc { 
    force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = false # If true disables the random hyprland logo / anime girl background. :(
    key_press_enables_dpms = true
    mouse_move_enables_dpms = true
    force_default_wallpaper = 0
    disable_splash_rendering = true
    animate_mouse_windowdragging = true
}

render {
    #explicit_sync = 0
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = hu,us
    kb_variant =
    kb_model =
    # Remap capslock to escape
    # kb_options = caps:swapescape
    kb_rules =

    follow_mouse = 1
    mouse_refocus = false

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = false
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
gestures {
    workspace_swipe = false
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


####################
### KEYBINDINGSS ###
####################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, RETURN, exec, uwsm app -s a -a terminal -- $terminal
bind = $mainMod SHIFT, Q, killactive,
bind = $mainMod SHIFT, X, exec, uwsm stop
bind = $mainMod, E, exec, uwsm app -- $fileManager
bind = $mainMod, F, togglefloating
bind = $mainMod, F, centerwindow
bind = $mainMod SHIFT, F, fullscreen
bind = $mainMod, D, exec, uwsm app -s a -a launcher -- $menu
bind = ,Print, exec, hyprshot --output ~/Képek/Screenshots -m region
bind = $mainMod, Print, exec, hyprshot --output ~/Képek/Screenshots -m output
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, J, togglesplit, # dwindle
bind = $mainMod, L, exec , hyprlock

bind = $mainMod, C, exec, hyprpicker -a
bind = $mainMod, O, exec, grim -g "$(slurp $SLURP_ARGS)" "tmp.png" && tesseract "tmp.png" - | wl-copy && rm -f "tmp.png"

# Tabs
bind = $mainMod, G, togglegroup

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d
bind = $mainMod SHIFT, right, movewindoworgroup, r
bind = $mainMod SHIFT, left, movewindoworgroup, l
bind = $mainMod SHIFT, up, movewindoworgroup, u
bind = $mainMod SHIFT, down, movewindoworgroup, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10
bind = $mainMod, T, workspace, name:T
bind = $mainMod, Z, workspace, name:Z
bind = $mainMod, U, workspace, name:U

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10
bind = $mainMod SHIFT, T, movetoworkspace, name:T
bind = $mainMod SHIFT, Z, movetoworkspace, name:Z
bind = $mainMod SHIFT, U, movetoworkspace, name:U

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Media keys
binde = ,XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
binde = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bind = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bind = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDI_SOURCE@ toggle


##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# workspace = name:T, decorate:false

# Example windowrule v1
# windowrule = float, ^(kitty)$

# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$

# windowrulev2=opacity 0.4,class:^(alacritty)$

windowrulev2 = suppressevent maximize, class:.* # You'll probably like this.

# file-roller
windowrulev2 = float,class:^(org.gnome.FileRoller)$
windowrulev2 = center,class:^(org.gnome.FileRoller)$

# steam
windowrulev2 = float,class:^(steam)$,title:^(Friends List)$

# Anydesk
windowrulev2 = noblur,class:^(Anydesk)$
#windowrulev2 = forceinput,class:^(Anydesk)$
windowrulev2 = noanim,class:^(Anydesk)$

# Firefox & PiP
windowrule = noblur, class:firefox # disables blur for firefox
windowrulev2 = float, class:(firefox), title:^(Picute-in-Picture)$
windowrulev2 = float, class:^(firefox)$, title:^(Kép a képben)$
windowrulev2 = pin, class:(firefox), title:^(Picute-in-Picture)$
windowrulev2 = pin, class:(firefox), title:^(Kép a képben)$
windowrulev2 = move 72% 72%, class:(firefox), title:^(Picute-in-Picture)$
windowrulev2 = move 72% 72%, class:(firefox), title:^(Kép a képben)$
windowrulev2 = size 25% 25%, class:(firefox), title:^(Picute-in-Picture)$
windowrulev2 = size 25% 25%, class:(firefox), title:^(Kép a képben)$


